<form method="post" action="{{ path('test_delete', {'id': r_e_s_e_r_v_a_t_i_o_n.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ r_e_s_e_r_v_a_t_i_o_n.id) }}">
    <button class="btn">Delete</button>
</form>
